FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
# -u forces unbuffered stdout so Cloud Run captures logs in real time.
CMD ["python", "-u", "main.py"]
