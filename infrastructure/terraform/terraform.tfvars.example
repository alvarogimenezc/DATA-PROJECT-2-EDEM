# ============================================================================
# terraform.tfvars.example — plantilla publica (SI se sube a git)
# ============================================================================
# Copia este fichero a `terraform.tfvars` (gitignored) y rellena los valores.
# NUNCA comitees `terraform.tfvars` porque contiene secretos.
#
# Como generar valores fuertes (cross-platform):
#   python -c "import secrets; print(secrets.token_hex(32))"
# ============================================================================

project_id       = "cloudrisk-492619"
region           = "europe-west1"
jwt_secret       = "docki"
scheduler_secret = "cracki"
